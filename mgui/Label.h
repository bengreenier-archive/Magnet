#ifndef LABEL_H
#define LABEL_H

#include "Component.h"
/*
class Label : virtual public Component
{
    public:
        Label(const char* name);
        Label(const char* name, const char* string);
        Label(const char* name, const char* string);

        virtual void SetSize();
        virtual void GetSize();



        virtual ~Label();
    protected:
    private:
        sf::String  m_string;
        float       m_string_width;
};
*/
#endif // LABEL_H
